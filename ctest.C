#include<iostream>
#include <iomanip>
#include <math.h>  

using namespace std;

int main(){
    
    //double pi, num1, num2;
    //pi = 3.14159265359;
    //num1 = 2.*pi;
    //num2 = 2*pi;
    
    //cerr << num1 << num2 <<  endl;
    //double sum = 0; 
    //double subt = 0;
    
    int i, j, ii, jj;
#if 0
    for(i=0; i<5; i++){
        for(j=0; j<3; j++){
            sum += i + j;
            cerr << " sum " << sum << endl;
            subt -= i - j;
            cerr << " subt: " << subt << endl;
        }
    }
    cerr << " sum: " << sum << " subt: " << subt << endl;
#endif
    
    i = 0;
    ii = 0;
    j=  0;
    jj = 1;
    bool production = 1;
    
    if((ii != i || jj != j) && production == true){
        cerr << " HERE " << endl;
    }
}
        
if all is gone good(then each bin of the histogram should be visited equally) == then
    if(bin_ii_new != bin_ii && wL->production == 1){
        
        increase the hist count ... with the bias increment
        
    }
    
    double count_hist, bias_hist;
    double min_hist_bias, min_hist_count;
    int num, min, sum;
    sum = 0;

    count_hist[3][3];
    bias_hist[2][2];
    for(int i=0; i<5; i++){
        for(int j=0; j<5; j++){
            sum += count_hist[i][j];
        }
    }
    
    count_hist{{1,2,3,4,5,6},
    {1,2,3,4,5,6},
    {2,3,4,5,6,7}};
    for(int i=0; i<3; i++){
        for(int j=0; j<3; j++){
            count_hist[i][j] = 0;
        }
    }
    
    








    


